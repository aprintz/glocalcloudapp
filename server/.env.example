# Azure Key Vault Configuration (Production)
AZURE_KEY_VAULT_URL=https://your-keyvault-name.vault.azure.net/

# Local Development Environment Variables (when Key Vault is not available)
# These will be used as fallbacks when AZURE_KEY_VAULT_URL is not set or in development
DATABASE_URL=postgres://user:password@localhost:5432/glocal
PORT=4000
DATABASE_SSL=disable
STRAPI_BASE_URL=http://localhost:1337
# STRAPI_TOKEN=optional_readonly_token
CMS_CACHE_TTL_MS=15000
APP_API_KEY=replace-with-a-strong-shared-secret

# Key Vault Secret Names (for reference):
# - database-url: Database connection string  
# - app-api-key: Shared secret for API authentication
# - strapi-token: Optional readonly token for Strapi API access
# - strapi-base-url: Base URL for Strapi CMS
